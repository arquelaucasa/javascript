let x = 10;
let y = 10; // para comparação sempre três cinal de iqual

console,console.log(x === y); // estamos testando se o valor de "x" é ugual ao valor de "y"
// as resposta serão true (para verdadeiro/afirmação) false(para falso/negaç~so)

let frutas = "Banana";
let frutas2 ="maça";
// aqui estamos perguntando se o valor da variável "frutas" é DIFERENTE da variável "frutas2"
console.log(frutas !== frutas2);

let numero = 20;
let segundoNumero = 258;
//estamos utilizando (maior que >) e (nenor que <) para comprar determinadas informações

console.log(numero >= 30); // ( > ) maior que ( < ) menor que 

// = (sinal de iqual significa "receba" )
// == ( dois sinal de iqual significa somente tipo )
// === (três sinal de iqual significa valor ou tipo )


